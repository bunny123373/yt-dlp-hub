services:
  - type: web
    name: yt-dlp-hub-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install && pip install yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/status/ping
